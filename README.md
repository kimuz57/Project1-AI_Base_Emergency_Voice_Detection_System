# Configuration
1.    install necessary library from package.json
```
npm i

2. create .env
```
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=ai_vd
```

# running website
1. run via command
```
npm run dev
```